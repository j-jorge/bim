#pragma once

namespace bim
{
#cmakedefine01 BIM_PROD_BUILD

#if BIM_PROD_BUILD && defined(NDEBUG)
  #define BIM_DEV 0
#else
  #define BIM_DEV 1
#endif

  constexpr bool built_for_developers = BIM_DEV;

#undef BIM_DEV
#undef BIM_PROD_BUILD
}
