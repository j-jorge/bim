#pragma once

#include <string_view>

namespace bim
{
#cmakedefine01 BIM_PURE_FOSS
#cmakedefine01 BIM_PROD_BUILD

#if BIM_PROD_BUILD && defined(NDEBUG)
  #define BIM_DEV 0
#else
  #define BIM_DEV 1
#endif

  constexpr std::string_view version = "@CMAKE_PROJECT_VERSION@"
#if BIM_PURE_FOSS
                                       " FOSS"
#endif
#if BIM_DEV
                                       " (dev)"
#endif
      ;

#undef BIM_DEV
#undef BIM_PROD_BUILD
#undef BIM_PURE_FOSS

  constexpr int version_major = @CMAKE_PROJECT_VERSION_MAJOR@;
}
